# 이 대화에서 입력한 프롬프트 모음

아래는 현재 대화 기록에서 `user_query`로 입력한 요청들을 시간순으로 모아둔 내용입니다.

## 1) 초기 기능 구현
> 바이브코딩 입문자를 위해 자바스크립트를 이용해서 웹앱에 일정관리를 위한 전형적인 필수 기능을 구현해줘

## 2) 아이콘/버튼 UI 수정
> 추가 버튼 앞에 의미를 알 수 없는 객체가 있어 그리고 다크 라이트 모드 버튼에 텍스트가 아닌 이미지로 라이트는 해 다크는 달님으로 변경해줘

## 3) 마감 입력 위치 개선
> 추가 앞에 있는 하얀 세로로 길죽한 하얀 버튼이 입력 시간을 넣는 버튼이구나. 문제는 데스크탑 해상도에서는 너무 작아서 어떤 기능의 버튼인지 알 수가 없어. 차라리 추가 버튼 위에 마감시간 입력칸을 옮기는 게 좋겠어

## 4) 대형 리팩터링 프롬프트
> 아래 3개 파일(index.html, style.css, script.js)로 구성된 MyToDo 앱을 검토해줘.
>
> 1. 디버깅: 실제 브라우저에서 오류 가능 지점 수정
> 2. 리팩토링:
> - script.js에서 `var`를 `const`/`let`으로 교체
> - `serializeFromDom` 기반 구조를 JS 배열 상태 관리 구조로 변경
> - 중복 코드는 함수로 묶기
> 3. 조건:
> - 기존 기능(입력/삭제/완료/필터/마감일/다크모드/localStorage) 유지
> - 파일 3개 분리 유지
> - 변경 부분마다 주석으로 이유 작성

## 5) DOM 흐름 시각화 패널 추가
> MyToDo 앱에 "DOM 흐름 시각화 패널"을 추가해줘.
>
> 목적: 사용자 조작 시 내부 기술 흐름을 실시간 표시  
> 흐름: 사용자 행동 -> 이벤트 발생 -> 함수 실행 -> DOM 변경 -> 화면 갱신  
> 조건:
> - 우측 하단 반투명 패널
> - 추가/삭제/완료/필터 변경 시 단계별 표시
> - 화살표(->)로 연결
> - 최근 5개 이벤트 유지
> - 열기/닫기 토글
> - 기존 기능은 건드리지 말고 `script.js`에만 기능 추가

## 6) 다크/라이트 아이콘 분리
> 좋아 이제 하나만 수정하자 다크라이트 모드 전환시 아이콘을 라이트일 때 해를 다크일 땐 달을 그려줘. 지금은 해와 달이 같이 있는 데 기능에 맞게 표시해줘

## 7) 테마 전환 시 아이콘 하드코딩 수정
> 라이크 모드일 때 해 아이콘을 다크모드일 땐 달 아이콘을 표시해줘야 하는 데 모드가 바껴도 해 아이콘으로 하드코드 되어 있어 모드에 따라 아이콘도 바뀌게 해줘

## 8) 하단 개발자 정보 추가
> 화면 하단에 정보 텍스트로 개발자 정보를 Made by JJorifong 이라고 적어줘

## 9) 반응형 디자인 일관성 개선
> 모바일 태블릿 데스크톱 ui 디자인을 좀 더 일관된 스타일로 변경해줘

## 10) UI 레이아웃 대규모 개편
> 현재 MyToDo 앱의 UI 레이아웃을 아래 기준으로 변경해줘.
>
> - 통계 패널 상단 이동(전체/완료/진행 중 3열)
> - 입력창 하단 고정(메신저형)
> - 모바일/태블릿/데스크톱 단일 열 통일
> - 1024px 이상 2열 제거
> - 기존 기능 유지 + CSS 토큰 재사용 + 변경 이유 주석

## 11) 통계 컬러 조정
> 상단 통계에서 완료부분의 컬러를 밝은 그린으로 해줘

## 12) GitHub 커밋 요청
> 이제 깃허브에 커밋해줘

## 13) README 수정 요청
> 리드미 파일 수정을 안했네. 리드미 파일 수정 작성해줘

## 14) 전체 파일 커밋 재요청
> 리드미만 반영된 듯 html jb css까지 모두 커밋해줘

## 15) 프롬프트 md 파일 생성 요청
> 프롬프트 md 파일을 생성해줘

## 16) 현재 요청
> 아니 이거 말고 ~ 지금 대화창에서 내가 입력했던 프롬프트 들을 모아서 md 파일로 만들어줘
