# MyToDo 앱 — 주요 프롬프트 기록

AI와 주고받은 핵심 요청을 단계별로 정리했습니다.  
세부 UI 조정·커밋·메타 요청은 제외하고, 앱 구조에 실질적 영향을 준 프롬프트만 남겼습니다.

---

## 1단계 — 초기 기능 구현

> 바이브코딩 입문자를 위해 자바스크립트를 이용해서 웹앱에 일정관리를 위한 전형적인 필수 기능을 구현해줘

**결과:** 할 일 추가·삭제·완료 체크·localStorage 저장 기본 구조 완성

---

## 2단계 — UI 레이아웃 전면 개편

> 현재 MyToDo 앱의 UI 레이아웃을 아래 기준으로 변경해줘.
>
> - 통계 패널 상단 이동 (전체/완료/진행 중 3열)
> - 입력창 하단 고정 (메신저형)
> - 모바일/태블릿/데스크톱 단일 열 통일
> - 1024px 이상 2열 제거
> - 기존 기능 유지 + CSS 토큰 재사용 + 변경 이유 주석

**결과:** 상단 통계 · 본문 목록 · 하단 고정 입력창 구조로 전환

---

## 3단계 — 리팩터링 + 디버깅

> 아래 3개 파일(index.html, style.css, script.js)로 구성된 MyToDo 앱을 검토해줘.
>
> 1. 디버깅: 실제 브라우저에서 오류 가능 지점 수정
> 2. 리팩터링:
>    - script.js에서 `var`를 `const`/`let`으로 교체
>    - `serializeFromDom` 기반 구조를 JS 배열 상태 관리 구조로 변경
>    - 중복 코드는 함수로 묶기
> 3. 조건:
>    - 기존 기능(입력/삭제/완료/필터/마감일/다크모드/localStorage) 유지
>    - 파일 3개 분리 유지
>    - 변경 부분마다 주석으로 이유 작성

**결과:** todos 배열 단일 상태(SSOT) 구조로 전환, 함수 최소 단위 분리 완료

---

## 4단계 — DOM 흐름 시각화 패널 추가

> MyToDo 앱에 "DOM 흐름 시각화 패널"을 추가해줘.
>
> 목적: 사용자 조작 시 내부 기술 흐름을 실시간 표시  
> 흐름: 사용자 행동 → 이벤트 발생 → 함수 실행 → DOM 변경 → 화면 갱신  
> 조건:
> - 우측 하단 반투명 패널
> - 추가/삭제/완료/필터 변경 시 단계별 표시
> - 화살표(→)로 연결
> - 최근 5개 이벤트 유지
> - 열기/닫기 토글
> - 기존 기능은 건드리지 말고 `script.js`에만 기능 추가

**결과:** 우하단 DOM 흐름 패널 설치, 조작별 이벤트→함수→DOM→화면 흐름 실시간 표시

---

## 5단계 — 드래그 앤 드롭 순서 변경

> MyToDo 앱(index.html, style.css, script.js)에 할 일 항목을
> 드래그 앤 드롭으로 순서를 바꾸는 기능을 추가해줘.
>
> 1. 기능
>    - 각 todo 항목을 마우스로 드래그해서 위아래 순서를 변경할 수 있어야 해
>    - 순서 변경 후 todos 배열 순서도 함께 업데이트되어야 해
>    - 변경된 순서는 localStorage에 저장되어 새로고침 후에도 유지되어야 해
>
> 2. 시각적 피드백
>    - 드래그 중인 항목은 반투명하게 표시해줘
>    - 드롭 가능한 위치에 구분선 또는 하이라이트를 표시해줘
>
> 3. 조건
>    - 외부 라이브러리 없이 HTML5 기본 Drag and Drop API만 사용해줘
>    - 기존 기능(추가/삭제/완료/필터/마감일/다크모드/통계/localStorage)은 그대로 유지해줘
>    - 마감일 기준 자동 정렬 기능과 충돌하지 않게 처리해줘
>      (마감일이 있는 항목은 정렬 우선, 없는 항목만 드래그 순서 적용)
>    - 변경한 부분마다 주석으로 이유를 한 줄씩 달아줘

**결과:** 마감일 없는 항목만 수동 재정렬 허용, order 값으로 localStorage에 순서 저장
